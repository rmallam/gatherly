# Frontend Staging Environment
# Used when running: npm run build -- --mode staging

# Staging backend URL (you can create a separate Render instance for staging)
VITE_API_URL=https://gatherly-backend-staging.onrender.com

# OR use production for now
# VITE_API_URL=https://gatherly-backend-3vmv.onrender.com
