# Frontend Environment Variables

# API URL - Backend server endpoint  
# Development: set to your local backend or staging URL (WITH /api suffix)
VITE_API_URL=http://localhost:3001/api

# The centralized config (src/config/api.js) uses this value directly
