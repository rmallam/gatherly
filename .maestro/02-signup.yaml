appId: com.guestscanner.app
---
# Test: User Login Flow (with Screenshots)
- launchApp:
    clearState: true

# Handle notification permission if it appears
- runFlow:
    when:
      visible: "Allow"
    commands:
      - tapOn: "Allow"

# Wait for app to load
- waitForAnimationToEnd

# Navigate to signup
- assertVisible: "Don't have an account?"
- tapOn: "Create Account"

# Should see signup form
- waitForAnimationToEnd
- assertVisible: "Full Name"

# Fill signup form with unique email
- tapOn:
    text: "John Doe"
- inputText: "Test User"
- tapOn:
    text: "your@email.com"
- inputText: "testuser${randomInt(1000,9999)}@example.com"
- tapOn:
    text: "Strong password"
- inputText: "Test@1234"

# Confirm password
- tapOn:
    text: "Confirm password"
- inputText: "Test@1234"

# Submit signup
- tapOn: "Create Account"

# Should show verification message or dashboard (if email verification skipped)
- waitForAnimationToEnd

# Success!
